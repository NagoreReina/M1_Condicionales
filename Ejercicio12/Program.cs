using System;

namespace Ejercicio12
{
    class Program
    {
        static void Main(string[] args)
        {
            //12. Escribe un programa que muestre la fecha de hoy
            //(pista 1: hay una función, pista 2: no hay que usar condicionales)

            Console.WriteLine(DateTime.Today.ToString("dd/MM/yyyy"));
        }
    }
}
